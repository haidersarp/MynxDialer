;===========================================================================
; http.conf — Plain HTTP/WebSocket for local Docker testing
; No SSL needed on localhost — browsers allow ws:// to localhost
;===========================================================================

[general]
enabled = yes
bindaddr = 0.0.0.0
bindport = 8088

; No TLS for local testing
tlsenable = no